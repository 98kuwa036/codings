<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Background -->
  <rect width="512" height="512" fill="#2563eb" rx="80"/>

  <!-- Warehouse Building -->
  <g transform="translate(256, 256)">
    <!-- Roof -->
    <path d="M -140 -80 L 0 -140 L 140 -80 L 140 -60 L -140 -60 Z"
          fill="#fff" opacity="0.95"/>

    <!-- Main Building -->
    <rect x="-140" y="-60" width="280" height="200"
          fill="#fff" opacity="0.95"/>

    <!-- Door (Large) -->
    <rect x="-80" y="30" width="70" height="110"
          fill="#2563eb" opacity="0.8"/>

    <!-- Door Lines -->
    <line x1="-80" y1="85" x2="-10" y2="85"
          stroke="#fff" stroke-width="3" opacity="0.5"/>

    <!-- Small Door -->
    <rect x="20" y="60" width="50" height="80"
          fill="#2563eb" opacity="0.8"/>

    <!-- Windows -->
    <rect x="-120" y="-30" width="30" height="30"
          fill="#93c5fd" opacity="0.9"/>
    <rect x="-120" y="20" width="30" height="30"
          fill="#93c5fd" opacity="0.9"/>
    <rect x="90" y="-30" width="30" height="30"
          fill="#93c5fd" opacity="0.9"/>
    <rect x="90" y="20" width="30" height="30"
          fill="#93c5fd" opacity="0.9"/>

    <!-- Box Stack (Package Icon) -->
    <g transform="translate(-50, -110)">
      <rect x="0" y="0" width="25" height="25"
            fill="#fbbf24" opacity="0.9" stroke="#fff" stroke-width="2"/>
      <rect x="10" y="-15" width="25" height="25"
            fill="#fbbf24" opacity="0.95" stroke="#fff" stroke-width="2"/>
      <line x1="10" y1="-15" x2="35" y2="-15" stroke="#fff" stroke-width="1.5"/>
      <line x1="22.5" y1="-15" x2="22.5" y2="10" stroke="#fff" stroke-width="1.5"/>
    </g>

    <!-- Checkmark Badge -->
    <circle cx="100" cy="-100" r="35" fill="#22c55e" opacity="0.95"/>
    <path d="M 85 -100 L 95 -90 L 115 -110"
          fill="none" stroke="#fff" stroke-width="8"
          stroke-linecap="round" stroke-linejoin="round"/>
  </g>
</svg>
