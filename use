# /etc/portage/make.conf

# 基本設定（Qt6/KDE/Wayland優先）
# 除外設定（GTK/GNOME/Systemd/Legacy排除）
USE="amd64 qt6 plasma wayland vulkan vaapi \
     alsa pipewire sound-server screencast \
     dbus elogind policykit udev \
     wifi \
     -gtk -gtk2 -gtk3 -gnome -eds -telepathy \
     -systemd -consolekit -pulseaudio \
     -doc -test -debug -handbook \
     -clang"
